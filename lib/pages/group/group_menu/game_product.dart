import 'package:flutter/material.dart'; 

class GameProduct extends StatelessWidget {
  const GameProduct({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('游戏周边');
  }
}