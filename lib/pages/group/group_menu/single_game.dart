import 'package:flutter/material.dart'; 

class SingleGame extends StatelessWidget {
  const SingleGame({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('单机游戏');
  }
}